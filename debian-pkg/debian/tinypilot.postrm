#!/bin/bash

# Exit script on first failure.
set -e

# Exit on unset variable.
set -u

# Disable TinyPilot's NGINX server and remove its config.
rm -f \
  /etc/nginx/sites-enabled/tinypilot.conf \
  /etc/nginx/sites-available/tinypilot.conf

# Reload NGINX config.
nginx -s reload || true

#DEBHELPER#
