#!/usr/bin/make -f

export DH_VIRTUALENV_INSTALL_ROOT=/opt/tinypilot/venv

%:
	dh $@  --with python-virtualenv

override_dh_installsystemd:
	dh_installsystemd --name=tinypilot-updater --no-start --no-enable
